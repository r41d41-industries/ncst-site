Create a self-contained modal dialog triggered by a single action button. Do not generate any surrounding page shell, sidebar, navbar, breadcrumb chrome, or adjacent content; output only the trigger button and its overlay modal so the component can be dropped into any host screen. **Trigger.** A single horizontally centered primary action button (such as "Create item") that opens the modal. **Overlay and dialog shell.** A fixed full-viewport overlay that centers a surface-card dialog constrained to a comfortable form width (a roomy two-column form) with a maximum width of 672 pixels. The dialog has internal padding, host-token rounded corners, the primary surface background, a soft elevation shadow, and scrolls internally when its content is tall. **Header.** A top row with a short title (such as "Add item") on the left and a small icon-only close button (X glyph with accessible label) on the right, separated from the body by a thin bottom divider. **Body.** A form laid out as a responsive grid that is one column on mobile and four columns from the small breakpoint up. Include, in order: a primary name text input spanning two columns; a secondary short text input spanning two columns; a numeric value input spanning two columns; a category-style single-select spanning two columns; then a row of four compact numeric inputs, each a single column wide, each with a small leading icon inside the field on the start edge and a muted placeholder (a cluster of measurement or attribute fields); a full-width multi-line description textarea; a full-width image upload dropzone rendered as a dashed-border drop area containing a centered upload glyph, a short instruction line, a max-file-size hint, and a primary "Browse file" button, paired with a visually hidden native file input; and a full-width inline single-choice radio group with three options on one baseline. Mark required fields with a small danger-colored asterisk beside the label. **Footer.** A top-divider-separated action row with a primary submit button carrying a small leading plus icon (such as "Add item"), a secondary outlined button carrying a small leading icon (such as a "Schedule" action), and a final secondary outlined dismiss button (such as "Close") pushed to the far end on wide viewports. **Sizing rules.** The dialog width stays bounded and never spans edge to edge. Leading field icons align to the input's vertical center. Surfaces, borders, dashed outlines, focus rings, dividers, and radii stay agnostic and follow the host design system; the required-asterisk and any semantic accents use host semantic tokens. **Button padding rule.** Every labeled action button (trigger, submit, secondary action, dismiss, and the dropzone "Browse" button) uses padding-left 16px, padding-right 16px, padding-top 10px, and padding-bottom 10px. This does not apply to the icon-only close button. **Input padding rule.** Standard text, numeric, and select fields use 12px horizontal and 10px vertical padding; icon-prefixed inputs reserve start padding for the leading icon; the textarea uses 14px of padding on all sides. **Select chevron padding rule.** Every single-select dropdown keeps at least 12 pixels of padding on its end (right) side so the dropdown chevron is never glued to the field's border; preserve that 12-pixel gap between the chevron and the edge and give the selected text enough trailing room that the value never sits beneath the chevron. **Input surface rule.** Fill every field control with a tint pulled from the same neutral tone as the dialog surface, nudged one step lighter — the identical hue and shade, only brighter, never a different color — so the fields separate from the dialog background through a soft, subtle contrast rather than a color shift. Apply this same fill uniformly across all text, numeric, icon-prefixed, and select inputs, the textarea, and the upload dropzone drop area, and keep the host default border (or dashed border for the dropzone) and focus ring so each field reads as a cleanly bounded, slightly lifted surface. **Mobile layout rule.** On narrow viewports the dialog grows toward the full available width, the four-column grid collapses to a single column in reading order (the four compact numeric inputs stack), the dropzone and inline radio group span full width, and the footer actions each stretch to full width and stack in primary-then-secondary-then-dismiss order.
